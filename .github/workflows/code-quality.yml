name: 📊 Code Quality Analysis

on:
  workflow_run:
    workflows: ["🧪 Continuous Integration"]
    types: [completed]
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]
  schedule:
    - cron: '0 6 * * 1' # Weekly on Monday at 6 AM UTC

env:
  NODE_VERSION: '20'
  PNPM_VERSION: '9.12.3'

jobs:
  # Job 1: Test Coverage Analysis
  coverage-analysis:
    name: 📈 Coverage Analysis
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: 📥 Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: 🔧 Generate Prisma Client
        run: |
          echo 'DATABASE_URL="postgresql://temp:temp@temp:5432/temp"' > .env
          pnpm prisma:generate
          rm .env

      - name: 📊 Generate detailed coverage
        env:
          DATABASE_URL: postgresql://temp:temp@temp:5432/temp
          JWT_SECRET: test-jwt-secret-for-ci
          JWT_REFRESH_SECRET: test-jwt-refresh-secret-for-ci
        run: pnpm test:unit-only --coverage

      - name: 📈 Coverage threshold check
        run: |
          echo "Checking coverage threshold..."
          # Add coverage threshold logic here if needed
